# Usa la imagen base oficial de MySQL
FROM mysql:latest

# Establece variables de entorno para la contraseña root y otras configuraciones de MySQL
ENV MYSQL_ROOT_PASSWORD=12345678
ENV MYSQL_DATABASE=apipoli
ENV MYSQL_USER=root
ENV MYSQL_PASSWORD=12345678

# Copia el script de inicialización a la carpeta de inicialización de MySQL
COPY init-db.sql /docker-entrypoint-initdb.d/

# Exponer el puerto MySQL
EXPOSE 3307

# El comando por defecto para ejecutar MySQL
CMD ["mysqld"]
